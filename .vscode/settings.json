{
    //-------- Mocha options --------
    "mocha.files.glob": "src/**/*.test.js",
    // Mocha: Environment variables to run your tests
    "mocha.env": {},
    "files.exclude": {
        "**/.git": true,
        "**/.DS_Store": true,
        "**/*.js.map": true,
        "**/*.js": {"when": "$(basename).ts"}
    }
}